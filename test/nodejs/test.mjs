import console from 'console'
import {buffer,internal} from 'node'
console.info('buffer is a =>', typeof buffer)
console.info('buffer.atob is a =>', typeof buffer.atob)
console.info('buffer.btoa is a =>', typeof buffer.btoa)
console.info('buffer.byteLengthUtf8 is a =>', typeof buffer.byteLengthUtf8)
console.info('buffer.compare is a =>', typeof buffer.compare)
console.info('buffer.compareOffset is a =>', typeof buffer.compareOffset)
console.info('buffer.copy is a =>', typeof buffer.copy)
console.info('buffer.indexOfBuffer is a =>', typeof buffer.indexOfBuffer)
console.info('buffer.indexOfNumber is a =>', typeof buffer.indexOfNumber)
console.info('buffer.indexOfString is a =>', typeof buffer.indexOfString)
console.info('buffer.swap16 is a =>', typeof buffer.swap16)
console.info('buffer.swap32 is a =>', typeof buffer.swap32)
console.info('buffer.swap64 is a =>', typeof buffer.swap64)
console.info('buffer.kMaxLength is =>', buffer.kMaxLength)
console.info('buffer.kStringMaxLength is =>', buffer.kStringMaxLength)
console.info('internal is a =>', typeof internal)
console.info('internal.util is a =>', typeof internal.util)
console.info('internal.util.constants.ALL_PROPERTIES is =>', internal.util.constants.ALL_PROPERTIES)
console.info('internal.util.constants.ONLY_ENUMERABLE is =>', internal.util.constants.ONLY_ENUMERABLE)
console.info('internal.util.getOwnNonIndexProperties is a =>', typeof internal.util.getOwnNonIndexProperties)
console.info('internal.util.isInsideNodeModules is a =>', typeof internal.util.isInsideNodeModules)

/* console.info('internal.buffer is a =>', typeof internal.buffer)
console.info('internal.errors is a =>', typeof internal.errors)
console.info('internal.errors.codes is a =>', typeof internal.errors.codes) */
/* 
console.info('internal.util.inspect is a =>', typeof internal.util.inspect)
console.info('internal.util.types is a =>', typeof internal.util.types)
console.info('internal.validators is a =>', typeof internal.validators) */
asdfasdfasdfasdfkljas;ldfkj;laskdjf;lksajd